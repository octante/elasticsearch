{% macro textarea(name, value, rows, cols, class) %}
    <textarea name="{{ name }}" rows="{{ rows|default(10) }}" cols="{{ cols|default(40) }}" class="{{ class|default("") }}">{{ value|e }}</textarea>
{% endmacro %}